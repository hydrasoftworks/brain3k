{
  "name": "brain3k-firebase",
  "description": "Local dependencies for Brain3k Firebase project",
  "scripts": {
    "serve": "firebase emulators:start",
    "deploy": "firebase deploy"
  },
  "engines": {
    "node": "14"
  },
  "devDependencies": {
    "firebase-tools": "^9.22.0"
  },
  "private": true
}
